<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="CustomControls.Pages.CounterAnimationsPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:skia="clr-namespace:SkiaSharp.Views.Forms;assembly=SkiaSharp.Views.Forms">
    <ContentPage.Content>
        <StackLayout
            HorizontalOptions="CenterAndExpand"
            Orientation="Vertical"
            VerticalOptions="CenterAndExpand">
            <Frame
                x:Name="counterFrame"
                Padding="0"
                HeightRequest="25"
                WidthRequest="25">
                <Grid
                    x:Name="counterGrid"
                    HorizontalOptions="CenterAndExpand"
                    VerticalOptions="CenterAndExpand">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="20" />
                    </Grid.RowDefinitions>

                </Grid>
            </Frame>
            <Button
                Clicked="OnStartAnimateClicked"
                Text="Animate"
                VerticalOptions="End" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>